# Copy to .env and fill in from your Supabase project
# (Dashboard → Project Settings → API). The anon key is safe to expose to
# the client — Row Level Security is what protects the data — but we keep
# .env out of git anyway.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
